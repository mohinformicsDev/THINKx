import { ImageDirectivesDirective } from './image-directives.directive';

describe('ImageDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
